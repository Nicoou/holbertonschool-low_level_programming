#include <stdio.h>
/**
 * main - start
 * Return: always 0
 *
 *
 */
int main(void)
{
	int x;
	int y;
	
	for (x =48; x <= 102; x++)
	{
	puchar(x);
	}
	for(y =97; y <= 102; y++)

	{
	puchar (y);
	}
	 puchar('\n');

	 return (0);
}
